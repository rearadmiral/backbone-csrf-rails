A simple monkey patch for Backbone to attach the CSRF meta tags used in Rails apps.

Assumes a jQuery-like $ is a global.

Probably you shouldn't use this and instead find a better one: https://github.com/shuvalov-anton/backbone-rails-sync